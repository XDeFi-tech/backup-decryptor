{
  "name": "backup-decryptor",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://xdefi-tech.github.io/backup-decryptor/",
  "dependencies": {
    "@types/node": "12.0.0",
    "@types/react": "17.0.0",
    "@types/react-dom": "17.0.0",
    "@xchainjs/xchain-crypto": "0.3.6",
    "bip39": "3.0.3",
    "crypto-browserify": "^3.12.1",
    "crypto-js": "4.2.0",
    "process": "^0.11.10",
    "react": "17.0.2",
    "react-app-rewired": "^2.2.1",
    "react-dom": "17.0.2",
    "react-scripts": "5.0.1",
    "stream-browserify": "^3.0.0",
    "typescript": "4.1.2",
    "vm-browserify": "^1.1.2"
  },
  "scripts": {
    "start": "NODE_OPTIONS=--openssl-legacy-provider react-app-rewired start",
    "build": "DEBUG=webpack:* NODE_OPTIONS=--openssl-legacy-provider react-app-rewired build --verbose",
    "test": "NODE_OPTIONS=--openssl-legacy-provider react-app-rewired test",
    "eject": "NODE_OPTIONS=--openssl-legacy-provider react-app-rewired eject",
    "predeploy": "NODE_OPTIONS=--openssl-legacy-provider yarn run build",
    "deploy": "NODE_OPTIONS=--openssl-legacy-provider gh-pages -b gh-pages -d build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@babel/plugin-proposal-private-property-in-object": "^7.21.11",
    "@types/crypto-js": "^4.0.1",
    "gh-pages": "^3.1.0"
  }
}
